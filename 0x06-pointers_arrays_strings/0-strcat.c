#include "main.h"
#include <string.h>

/**
 * _strcat - main block
 * @dest: string
 * @src: string
 * Return: pointer to strings concat
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
