#include <stdio.h>

/**
 * main - mainblock
 * @argc: number of arguments
 * @argv: arg values
 * Return: 0
 */

int main(int argc, char __attribute__((unused)) **argv)
{
	printf("%d\n", (argc - 1));
	return (0);
}
